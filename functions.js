export function lookup_policy(section) {
  return `Looking up section: ${section}`;
}

export function cite_source(doc, section) {
  return `${doc}, Section ${section}`;
}

export function summarize_document(doc) {
  return `Summary of ${doc}`;
}
